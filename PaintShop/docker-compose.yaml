version: '3.7'

services:
    web:
      build: .
      ports:
        - "8080:8080"
  mysql:
    image: mysql:8
    ports:
      - 3306:3306
    volumes:
      - ~/apps/mysql:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: 12345
      MYSQL_DATABASE: '[personal_data_base]'
