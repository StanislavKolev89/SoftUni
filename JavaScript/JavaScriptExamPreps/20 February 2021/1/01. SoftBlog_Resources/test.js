function test(){

    let button = document.getElementById('button');
    button.addEventListener('click',sort);

    function sort(e){
        let list = document.querySelectorAll('sort');
        
    }
}